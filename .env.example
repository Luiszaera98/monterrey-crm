# Environment Variables for Monterrey CRM

# Database Credentials (REQUIRED)
# Change these to strong values for production!
DB_USER=admin
DB_PASSWORD=change_this_to_a_secure_password

# Next.js Configuration
# In production, this should be your domain name (e.g., https://myapp.com)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MongoDB Connection String (calculated automatically in docker-compose, but useful for reference)
# MONGODB_URI=mongodb://${DB_USER}:${DB_PASSWORD}@mongodb:27017/monterrey_crm?authSource=admin
